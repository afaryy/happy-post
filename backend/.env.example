# Safe local configuration only. Do not place real credentials in this file.
APP_VERSION=0.1.0
APP_TIMEZONE=Australia/Melbourne
SESSION_TTL_SECONDS=604800
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/DBNAME
# Local Compose default only; do not reuse for AWS or shared environments.
# DATABASE_URL=postgresql+psycopg://happy_post:happy_post_local@localhost:5432/happy_post
